import React from "react";
import {Header} from "../components/Header";

export const About = (props) => {

    return(
        <div className = "container">
            <h3>About React Application</h3>
            <Header />

        </div>

    );

}