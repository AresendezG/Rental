import React from "react";
import {Header} from "../components/Header";

export const Help = (props) => {

    return(
        <div className = "container">
            <h3>Utilice los siguientes Links para crear un nuevo registro</h3>
            <Header />
        </div>
    );

}
